<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.airjnc.user.dao.UserDAO">
    <select id="selectUserById" parameterType="Long" resultMap="userResultMap">
        select id,
               email,
               password,
               name,
               gender,
               phone_number,
               address,
               is_active,
               birthdate,
               created_at,
               updated_at,
               deleted_at
        from user
        where id = #{id}
    </select>

    <select id="selectUserByEmail" parameterType="String" resultMap="userResultMap">
        select id,
               email,
               password,
               name,
               gender,
               phone_number,
               address,
               is_active,
               birthdate,
               created_at,
               updated_at,
               deleted_at
        from user
        where email = #{email}
    </select>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into user
            (email, password, name, gender, phone_number, address, birthdate)
        values (#{email}, #{password}, #{name}, #{gender}, #{phoneNumber}, #{address}, #{birthDate})
    </insert>

    <resultMap id="userResultMap" type="User" autoMapping="true">
        <result property="active" column="is_active"/>
    </resultMap>
</mapper>
    